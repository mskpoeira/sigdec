"use client";
import Link from "next/link";import{FormEvent,useCallback,useEffect,useState}from"react";
const API=process.env.NEXT_PUBLIC_SIGDEC_API_URL??"http://localhost:4000";
export default function Page(){const[items,setItems]=useState<any[]>([]),[msg,setMsg]=useState(""),[busy,setBusy]=useState(false);
const load=useCallback(()=>fetch(`${API}/api/v1/humanitarian/households`,{credentials:"include"}).then(async r=>{if(r.status===401){location.href="/login";return null}return r.ok?r.json():null}).then(b=>b&&setItems(b.items??[])),[]);
useEffect(()=>{load()},[load]);
async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMsg("");const f=new FormData(e.currentTarget);const body={responsibleName:String(f.get("responsibleName")||""),phone:String(f.get("phone")||"")||undefined,condition:String(f.get("condition")),adults:Number(f.get("adults")||0),children:Number(f.get("children")||0),elderly:Number(f.get("elderly")||0),personsWithDisability:Number(f.get("pcd")||0),addressOrigin:String(f.get("addressOrigin")||"")||undefined,notes:String(f.get("notes")||"")||undefined};const r=await fetch(`${API}/api/v1/humanitarian/households`,{method:"POST",credentials:"include",headers:{"content-type":"application/json"},body:JSON.stringify(body)});setBusy(false);if(!r.ok){setMsg("Não foi possível registrar a família.");return}e.currentTarget.reset();setMsg("Família registrada.");await load()}
return <main className="shell"><header className="listHeader"><div><span className="eyebrow">SIGDEC · v1.1</span><h1>Assistência Humanitária</h1><p>Famílias desalojadas e desabrigadas, abrigos, estoque e entregas.</p></div><Link className="secondaryLink" href="/painel">Painel</Link></header><section className="operationsGrid"><form className="incidentForm compactForm" onSubmit={submit}><h2>Registrar família</h2><label>Responsável<input name="responsibleName" required/></label><label>Telefone<input name="phone"/></label><label>Condição<select name="condition"><option value="DISPLACED">Desalojada</option><option value="HOMELESS">Desabrigada</option></select></label><div className="formGrid"><label>Adultos<input name="adults" type="number" min="0" defaultValue="0"/></label><label>Crianças<input name="children" type="number" min="0" defaultValue="0"/></label><label>Idosos<input name="elderly" type="number" min="0" defaultValue="0"/></label><label>PCD<input name="pcd" type="number" min="0" defaultValue="0"/></label></div><label>Endereço de origem<input name="addressOrigin"/></label><label>Observações<textarea name="notes"/></label>{msg&&<p className="formMessage">{msg}</p>}<button className="primaryButton" disabled={busy}>{busy?"Salvando...":"Registrar"}</button></form><section className="dataGrid">{items.length===0?<div className="infoCard">Nenhuma família registrada.</div>:items.map(x=><article className="card" key={x.id}><h2>{x.responsibleName}</h2><p><strong>{x.condition==="HOMELESS"?"Desabrigada":"Desalojada"}</strong>{x.protocol?` · ${x.protocol}`:""}</p><p>Adultos: {x.adults} · Crianças: {x.children} · Idosos: {x.elderly} · PCD: {x.personsWithDisability}</p><p>{x.shelterName?`Abrigo: ${x.shelterName}`:"Sem abrigo vinculado"}</p></article>)}</section></section></main>}