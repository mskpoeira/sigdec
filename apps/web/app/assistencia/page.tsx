"use client";
import Link from "next/link";
import { useEffect,useState } from "react";
const API=process.env.NEXT_PUBLIC_SIGDEC_API_URL??"http://localhost:4000";
export default function Page(){const [items,setItems]=useState<any[]>([]);const [status,setStatus]=useState("Carregando...");
useEffect(()=>{fetch(`${API}/api/v1/humanitarian/households`,{credentials:"include"}).then(async r=>{if(r.status===401){location.href="/login";return null}if(!r.ok)throw new Error();return r.json()}).then(b=>{if(b){setItems(b.items??[]);setStatus("")}}).catch(()=>setStatus("Não foi possível carregar os dados."))},[]);
return <main className="shell"><header className="listHeader"><div><span className="eyebrow">SIGDEC · v0.5</span><h1>Assistência Humanitária</h1><p>Famílias desalojadas/desabrigadas, abrigos e atendimento humanitário.</p></div><div className="headerActions"><Link className="secondaryLink" href="/painel">Painel</Link></div></header>
{status&&<section className="infoCard">{status}</section>}<section className="dataGrid">{items.length===0&&!status?<div className="infoCard">Nenhum registro cadastrado.</div>:items.map((x:any)=><article className="card" key={x.id}><h2>{x.responsibleName}</h2><p><strong>{x.condition==="HOMELESS"?"Desabrigada":"Desalojada"}</strong>{x.protocol?` · ${x.protocol}`:""}</p><p>Adultos: {x.adults} · Crianças: {x.children} · Idosos: {x.elderly} · PCD: {x.personsWithDisability}</p><p>{x.shelterName?`Abrigo: ${x.shelterName}`:"Sem abrigo vinculado"}</p></article>)}</section></main>}